let head = document.getElementById("head");
let h1 = head.innerHTML;
console.log(h1); //Template literal

console.log(`text is:${h1}`);  //text is:Template literal
console.log(`Sum is:${2 + 3}`);  //Sum is:5 we can do mathematical calculation